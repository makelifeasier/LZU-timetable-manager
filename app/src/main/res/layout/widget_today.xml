<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_bg"
    android:orientation="vertical"
    android:paddingStart="12dp"
    android:paddingEnd="12dp"
    android:paddingTop="8dp"
    android:paddingBottom="4dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical">

            <TextView
                android:id="@+id/widget_title"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textColor="@color/widget_text"
                android:textSize="15sp"
                android:textStyle="bold" />

            <!--
              副标题：`第 N 周  ·  接下来 X 节  ·  每日一句` —— 每日一句拼在**这一行**上
              （用户要求："把话放在第 n 周接下来第 n 节同一行"），于是它既不占列表的一格、
              也不占列表下方一条，一个像素的位置都不多花。

              maxLines=1 + ellipsize=end 是这里的关键：句子长了宁可截断，
              也**绝不能换行** —— 一换行头部就变高，整块组件的行数与图片高度全跟着跳。
            -->
            <TextView
                android:id="@+id/widget_subtitle"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="1dp"
                android:ellipsize="end"
                android:maxLines="1"
                android:textColor="@color/widget_subtext"
                android:textSize="11sp" />
        </LinearLayout>

        <!--
          模式切换：接下来 / 整天。
          这里原来是 30dp 的方形图标（ic_widget_list），用户根本看不出那是什么 ——
          实测反馈是"右下角一个正方形不知道干嘛的"，还容易和图片占位符搞混。
          改成写着当前模式的文字胶囊：一眼知道现在看的是哪一种，点一下切换。
        -->
        <TextView
            android:id="@+id/widget_mode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_pill"
            android:gravity="center"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:paddingTop="5dp"
            android:paddingBottom="5dp"
            android:textColor="@color/widget_now"
            android:textSize="11sp" />

        <TextView
            android:id="@+id/widget_refresh"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingStart="6dp"
            android:paddingEnd="2dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"
            android:text="@string/widget_refresh"
            android:textColor="@color/widget_now"
            android:textSize="12sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!--
      可滚动列表：用 ListView + RemoteViewsService 是安卓可滚动小组件的唯一正路。
      高度在运行时按「能放下几个整行」精确设置（见 TodayWidgetProvider.build），
      这样不会出现被切掉一半的第三行。这里的 94dp = 2 行，只是取不到尺寸时的兜底。

      图片开着时，行数是**先扣掉图片预留高度**再算的（WidgetData.rowsFor / photoReserveDp）：
      于是同一档高度里拖动组件，行数在一整行之内不变。而排完整行之后剩下的高度（含那条不足
      一整行的零头）**全部归图片框**（ExtrasPlanner.plan）—— 组件底部因此不会留白。
    -->
    <ListView
        android:id="@+id/widget_list"
        android:layout_width="match_parent"
        android:layout_height="94dp"
        android:layout_marginTop="8dp"
        android:cacheColorHint="@android:color/transparent"
        android:divider="@null"
        android:dividerHeight="0dp"
        android:listSelector="@android:color/transparent"
        android:overScrollMode="never"
        android:requiresFadingEdge="none"
        android:scrollbars="none" />

    <!--
      这里不再放加权占位控件（Space / View）：两者都没有 @RemoteView 注解，
      不在 RemoteViews 白名单内，桌面 inflate 会直接失败、小组件变空白。
      列表高度已经按整行精确计算，余量只剩几 dp，不需要占位。
    -->

    <TextView
        android:id="@+id/widget_empty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp"
        android:text="@string/widget_no_class"
        android:textColor="@color/widget_subtext"
        android:textSize="12sp"
        android:visibility="gone" />

    <!--
      页脚（原来显示「X 分钟前同步 · 可上下滑动」）**这一行删掉了**（用户要求）。

      控件本身留着是为了 `app/src/debug/**` 的自检还在 `findViewById(R.id.widget_footer)`
      （那个目录不允许改动），但代码里**永远把它设为 GONE、也不设文字** —— 所以它不占位、
      不显示。它原来的 22dp 已经还给布局：`WidgetData.chromeDp` / `rowsFor` 现在一律按
      「头部 49dp」算（不再有"矮组件才收页脚"这一档），每一档高度要么多一行课、要么图片高 22dp。
    -->
    <TextView
        android:id="@+id/widget_footer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:textColor="@color/widget_subtext"
        android:textSize="10sp"
        android:visibility="gone" />

    <!--
      每日一句**也不在这里**：它拼在上面的副标题那一行（`第 N 周 · 接下来 X 节 · 句子`），
      见 WidgetData.subtitleText 与 TodayWidgetProvider.build。
      既不占列表的一格、也不占列表下方一条 —— 用户要的就是"同一行"。
    -->

    <!--
      图片框：一个 FrameLayout 包一个 ImageView。
        · 高度由代码按「列表下方**剩下的全部空间** − 6dp 上边距」用 setViewLayoutHeight 设置
          （见 ExtrasPlanner.plan / WidgetExtras.apply；单位是**像素**，与位图逐像素一致）：
          课程行数先按整行定死，剩下的零头归框，所以组件底部不会漏出一条空白
          （判据：日志里的 `底部零头=0dp`）；
        · **整块（这个 FrameLayout）都可点** → 换下一张（Provider 的 ACTION_NEXT_PHOTO 广播）。
          点挂在这一层而不是里面的 ImageView：ImageView 默认不消费点击，事件照样落到这里，
          所以"图还没解出来/留白"时点上去也有反应；这块的高度就等于图片的高度，
          不会多占课程列表的位置，也不会抢列表的手势（ListView 自己处理自己的滑动）。

      **这里不再有 AdapterViewFlipper**。曾经用它做自动轮播（宿主自己翻页），
      但它是个可滚动的 AdapterView：竖直手势被它吃掉，课程列表就滑不动了 ——
      真机反馈"自动轮播启动后小组件无法使用"就是它。现在自动换图改由 PhotoAutoAdvance
      定时局部重画这一个 ImageView，整块布局里没有任何会吃手势的轮播控件。

      ## scaleType="fitCenter" 是恒等变换，不是随便选的

      代码用 PhotoFit.layout 产出的位图**逐值等于这块框的像素尺寸**（填满、零留边），
      而位图内部是这样来的（[PhotoBitmap.decode]）：从（用户已经裁好的）源图里取一块
      **与框同比例、居中、尽可能大**的矩形，缩到框的像素尺寸 —— **只画这一层**。

        · 比例一致（用户按裁剪框裁出来的照片落进与它同形的框）时，取到的就是**整张源图**，
          一个像素都不裁，日志写「整张(铺满)」；
        · 比例不一致（组件被拉高/压扁之后）时取源图内部一块，日志写「取中间块」。

      **不要在这里、也不要在 decode 里加第二层背景。** 曾经为了让"既不裁也不留白"两全，
      在照片四周铺过一层"同一张照片缩到 1/8 再放大"的**模糊底** —— 用户实测否掉了：
      放大后能看见**块状色块**（"图片上下怎么有模糊的像素图片块"）。
      现在的取舍是**宁可多裁一点也要画面干净**，而"裁多少"由用户在导入时的裁剪界面自己掌握
      （裁剪框的形状 = 当时组件图片区的形状，所以正常用下来比例是对得上的）。

      两种产出的宽高都等于框，所以 fitCenter 既不缩放也不裁剪（centerCrop 同样是恒等变换，
      但 fitCenter 是更弱的假设：万一哪天位图比框小了一点，centerCrop 会放大着裁，更容易看出问题）。
      宿主端因此不需要任何 setInt("setScaleType")，渲染结果只由我们自己产出的位图决定。

      照片的**四个圆角**也是在位图里做好的（PhotoBitmap.maskCorners，半径读的就是这块背景 drawable
      的 <corners>）—— 否则照片的直角会压在这块圆角背景上，就是用户说的"直角紧贴圆角，很突兀"。
    -->
    <FrameLayout
        android:id="@+id/widget_photo_area"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="6dp"
        android:background="@drawable/widget_row_bg"
        android:visibility="gone">

        <ImageView
            android:id="@+id/widget_photo_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:contentDescription="@string/widget_photo_desc"
            android:scaleType="fitCenter" />
    </FrameLayout>
</LinearLayout>
